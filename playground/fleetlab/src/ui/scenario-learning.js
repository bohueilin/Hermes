/** Teaching context and result prose. Decisions remain recorded by each model's contract. */
import {el} from './dom.js';
const decisions={
  day:{id:'day',question:'Where is vehicle time being lost before the next rider?',change:'Start with the regular day, busy depot, rainy peak or limited-power situation; name the assumption you change.',hold:'For a causal comparison, hold demand seed, places, fleet profiles and all other settings fixed.',watch:'Completed / all requests, unserved, waiting, in-progress trips, queues and terminal energy.',meaning:'Find the limiting resource before buying more fleet or depot capacity. A busy resource is not automatically the constraint on rider service.',next:'Use the fleet/depot comparison, then isolate a worker, bay or power change.',limits:'One replay is descriptive. Capacity trials add complete sites; their 95% target is end-of-window completion, not a pickup promise.'},
  staffing:{id:'staffing',question:'Would a qualified cleaning worker return more vehicles to riders?',change:'Compare staffing and bays: one extra worker; separately, one extra bay.',hold:'Same demand, required-work rule, fleet, geography, energy and all other resources.',watch:'Completed / all requests; all-visit queue and active time; unfinished tasks and worker/bay blockers.',meaning:'An empty bay cannot perform required cleaning without a worker. Adding a nonbinding resource can leave service unchanged.',next:'Inspect blocked task-minutes, then test the other resource while holding the first fixed.',limits:'Serial work and one cleaning skill. Every request and unfinished visit remains counted. One seed is descriptive.'},
  charging:{id:'charging',question:'Can unused power shares help another car become ready?',change:'Choose charging redistribution: equal-share allocation versus feasible redistribution.',hold:'Same ports, site cap, vehicle acceptance, targets, queue order and external demand.',watch:'Power delivered, readiness deadlines, queue versus connected time, all-request service and terminal energy.',meaning:'Occupied plugs and useful power are different measures. Recovering unused power does not guarantee more completed service.',next:'Inspect which acceptance cap binds; separately test charging deadlines against redistribution.',limits:'Battery-side linear charging; no taper, losses, tariffs or battery-life claim. A single replay has no statistical confidence.'},
  deadlines:{id:'deadlines',question:'Does prioritizing earlier readiness deadlines help service without starving other work?',change:'Choose charging deadlines: redistribution versus deadline priority with aged-job protection.',hold:'Same declared deadlines, energy needs, ports, site power and external demand.',watch:'Ready by deadline, missed/pending deadlines, aged jobs, unfinished work and terminal energy.',meaning:'Reordering work can trade one vehicle readiness time against another. Inspect every guardrail alongside completed service.',next:'Vary the declared deadline budget in a separate experiment; retain the same treatment within each comparison.',limits:'Illustrative vehicle-ID deadline tiers, not real appointments. Aged-job priority does not guarantee every job completes under overload.'},
  resources:{id:'resources',question:'Can the planner avoid counting a charger whose status is stale?',change:'Choose resource freshness: last-known eligibility versus freshness-aware eligibility.',hold:'Same physical outage, observation timing, ports, power, fleet and external demand.',watch:'Unknown/stale state, false-ready port-minutes, rejected proposals, recovery delay and service.',meaning:'A planner can propose an impossible assignment while the environment correctly rejects it. Fresher eligibility can reduce rejected work without improving throughput.',next:'Change observation delay separately; compare policies again with the same outage in both arms.',limits:'Sequenced synthetic observations, not a connected status feed. Missing observations never establish usable capacity.'},
  airport:{id:'airport',question:'Can preparing supply for an airport wave protect pickups without draining other areas?',change:'Choose airport preparation: reactive service versus time-valid advance preparation.',hold:'Same realized passenger wave, ride conversion, fleet, energy and staging capacity.',watch:'Airport pickups within target / all airport-origin requests; late and pending pickups; other-area service, staging, terminal energy and work.',meaning:'Better airport coverage can still accompany worse system outcomes. A HOLD keeps those trade-offs visible.',next:'Change the declared wave estimate separately to test accuracy; the policy cannot inspect future realized requests.',limits:'Synthetic SFO scenario and fictional dated access rule. No real passenger-demand or curb-access claim.'},
  launch:{id:'launch',question:'How much service is lost when planned charging capacity is commissioned late?',change:'Rehearse immediate commissioning versus a declared commissioning delay in the same region.',hold:'Same region, fixed site/resource identities, fleet, demand, task rules, power and seed.',watch:'Setup blockers and owners; planned/installed/commissioned/usable ports; pickups within target; every request, unfinished work and energy.',meaning:'A configuration is reusable across regions, but equipment is capacity only when its prerequisites and usable-state checks hold.',next:'Try Region B with the same workflow, then vary commissioning delay or a site resource in a separate rehearsal.',limits:'Synthetic launch rehearsal and local-minute calendars. Setup checks are not inspections, permits or authorization. One seed is descriptive.'},
};
export function decisionForConfig(c){return decisions[c.launch?'launch':c.airport?'airport':c.resources?'resources':c.charging?.policy==='deadline'?'deadlines':c.charging?'charging':c.readiness?'staffing':'day'];}
export function decisionView(d){return el('section',{class:'ops-decision-guide','aria-label':'Scenario learning guide'},[
  el('p',{class:'eyebrow'},'Fleet optimization decision'),el('h2',{},d.question),
  el('dl',{},[['Test',d.change],['Hold fixed',d.hold],['Read the result',d.watch],['Why it matters',d.meaning],['Next experiment',d.next]].flatMap(([k,v])=>[el('dt',{},k),el('dd',{},v)])),
  el('p',{class:'ops-settings-note'},d.limits),
]);}
export function comparisonLearning(r){
  if(r.validity==='INVALID_EXPERIMENT')return `This comparison is unavailable: ${r.reason}. Resolve the contract or missing population before interpreting a difference.`;
  if(r.descriptive)return 'One seed shows what happened in this synthetic run. Inspect both arms and unfinished work, then use separate evaluation seeds before interpreting a repeatable effect.';
  if(!r.analysis)return 'No comparative conclusion is available.';
  const a=r.analysis,guards=(a.guardrail_statuses??[]).filter(g=>g.status!=='WITHIN');
  const interpretation={IMPROVED:'The primary outcome improved beyond the declared margin in this model.',REGRESSED:'The primary outcome regressed beyond the declared margin in this model.',UNCHANGED:'The paired interval stayed within the declared practical margin; this is not proof that every operating measure is identical.',INCONCLUSIVE:'The interval does not resolve the declared practical difference. More repetitions or a better-isolated mechanism may be useful.'}[a.outcome]??'Inspect the recorded primary and guardrail results.';
  return `${interpretation} Recorded recommendation: ${a.recommendation}. ${guards.length?'Inspect these guardrails: '+guards.map(g=>`${g.metric} (${g.status})`).join(', ')+'. A primary improvement cannot compensate for them.':'Review terminal energy and unfinished work alongside the primary outcome.'}`;
}
