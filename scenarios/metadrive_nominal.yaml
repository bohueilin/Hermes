schema_version: "1.0"
name: metadrive_nominal
version: "1.0"
description: Deterministic-seed nominal MetaDrive 0.4.3 headless scenario on a fixed straight map.
adapter: metadrive
control:
  frequency_hz: 10
  horizon_steps: 300
  target_speed_mps: 8.0
initial_state:
  speed_mps: 0.0
  lateral_offset_m: 0.0
road:
  destination_distance_m: 20.0
  boundary_tolerance_m: 1.5
hazards: {}
